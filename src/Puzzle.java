public class Puzzle {
	public static void main(String args[])
	{
		/* FOR 1..12 initialize a new layout with a new input file... for the different levels 
		 * WHEN level is DONE iterate */

		Layout L = new Layout(1); //pass a number corresponding to the level number to be loaded, i.e. for "level1.txt" pass the value "1"
	}

}
